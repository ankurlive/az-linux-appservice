# Auth only — edit locals.tf for app / container / nginx config

usr_subscription_id = "your-subscription-id"
usr_tenant_id         = "your-tenant-id"
usr_client_id         = "your-client-id"
usr_client_secret     = "your-client-secret"
